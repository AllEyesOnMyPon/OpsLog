# ===============================
# S13: Konwencje ENV (jednoznaczne)
# ===============================

# HMAC dla klientów/emitera (AuthGW):
LOGOPS_API_KEY=
LOGOPS_SECRET=

# Punkt wejścia (domyślnie AuthGW /ingest) – używają go emitery:
ENTRYPOINT_URL=http://127.0.0.1:8081/ingest

# Ingest -> Core (forward URL używany przez Ingest):
CORE_URL=http://127.0.0.1:8095/v1/logs

# NDJSON sink (spójny katalog dla Ingest/Core):
LOGOPS_SINK_DIR=./data/ingest

# ===============================
# Pozostałe ustawienia (opcjonalne)
# ===============================
# Ingest / Core – dodatkowe flagi:
CORE_SINK_FILE=false
CORE_SINK_DIR=              # jeżeli nie pusty – nadpisze LOGOPS_SINK_DIR w Core
CORE_MAX_BODY_BYTES=1048576
CORE_MAX_ITEMS=5000

# Ingest – (jeśli masz swoje flagi w metrics.py itp.)
LOGOPS_SECRET_KEY=H0JFlLBAZirMvzzQD-lQziWhRGesRXWQMK1nA1u5b5k=
LOGOPS_ENCRYPT_PII=true
LOGOPS_ENCRYPT_FIELDS=user_email,client_ip
LOGOPS_DEBUG_SAMPLE=true
LOGOPS_DEBUG_SAMPLE_SIZE=3
LOGOPS_HOUSEKEEP_AUTORUN=true
LOGOPS_HOUSEKEEP_INTERVAL_SEC=0

# Alertmanager / Slack (dev):
ALERTMANAGER_SLACK_WEBHOOK=
ALERTMANAGER_SLACK_WEBHOOK_LOGOPS=
